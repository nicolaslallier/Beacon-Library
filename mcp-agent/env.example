# LM Studio Configuration
LMSTUDIO_URL=http://192.168.2.35:1234
LMSTUDIO_MODEL=default

# Beacon Library MCP Configuration
# Option 1: Use direct HTTP port (recommended for local network)
BEACON_MCP_URL=http://localhost:8200/mcp

# Option 2: Use HTTPS with self-signed cert (set VERIFY_SSL=false)
# BEACON_MCP_URL=https://beacon-library.famillelallier.net/api/mcp
# VERIFY_SSL=false

AGENT_ID=lmstudio-agent

# SSL verification (set to false for self-signed certificates)
VERIFY_SSL=true

# Server Configuration
HOST=0.0.0.0
PORT=8080
